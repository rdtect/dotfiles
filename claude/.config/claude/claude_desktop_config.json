{
  "mcpServers": {
    "github": {
      "command": "/home/rdtect/.nvm/versions/node/v25.2.1/bin/npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub operations - PRs, issues, repos, code search. Set GITHUB_TOKEN in ~/.zshrc"
    },
    "knowledge-graph": {
      "command": "/home/rdtect/.nvm/versions/node/v25.2.1/bin/npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent knowledge graph - entities, relations, observations across sessions"
    },
    "filesystem": {
      "command": "/home/rdtect/.nvm/versions/node/v25.2.1/bin/npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/home/rdtect",
        "/home/rdtect/Projects",
        "/mnt/d/rdtect"
      ],
      "description": "Filesystem: home, projects, and Obsidian vault (/mnt/d/rdtect)"
    },
    "coolify": {
      "command": "/home/rdtect/.nvm/versions/node/v25.2.1/bin/npx",
      "args": ["-y", "@joshuarileydev/coolify-mcp-server"],
      "env": {
        "COOLIFY_API_URL": "https://coolify.rdtect.com",
        "COOLIFY_API_TOKEN": "5|rdos-coolify-mcp-2026-token",
        "COOLIFY_TEAM_ID": "0"
      },
      "description": "Coolify VPS management - services, apps, deployments"
    },
    "chroma": {
      "command": "/usr/sbin/uvx",
      "args": [
        "chroma-mcp",
        "--client-type", "http",
        "--host", "chroma.rdtect.com",
        "--port", "443",
        "--custom-auth-credentials", "rdos-chroma-2026-token",
        "--ssl", "true"
      ],
      "description": "ChromaDB semantic search at chroma.rdtect.com - 8537 chunks from Obsidian vault. Route added via Traefik."
    },
    "obsidian": {
      "command": "/home/rdtect/.nvm/versions/node/v25.2.1/bin/npx",
      "args": ["-y", "mcp-remote", "http://localhost:22360/sse"],
      "description": "Obsidian vault via iansinnott/obsidian-claude-code-mcp plugin. Requires: 1) Plugin installed in Obsidian, 2) socat bridge running (auto-starts via .zshrc), 3) Obsidian open on Windows"
    }
  }
}
